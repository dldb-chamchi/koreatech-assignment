package org.example.domain.user.pensionManager;

import org.example.domain.user.User;

public class PensionManager extends User {
    public PensionManager(String name, String accountId, String password, String phone, String email) {
        super(name, accountId, password, phone, email);
    }
    
}
