<사건 정보 유지>
처리된 사건은 지워져야함, 이벤트들이 시간 순서대로 입력되어있음
우선순위 큐를 사용, 이벤트들이 들어올 때 시간 순으로 자료구조에 넣을 수 있음
이벤트가 지나가면 pop하여 지울수 있음

PriorityQueue<Integer> event = new PriorityQueue<>();

<탑승객 목록>
지우기가 용이해야함
map 구조로 키 값은 층 수, value값은 층에서 내릴 사람 수 or 사람 이름 자체(list로 유지)를 유지
내릴 층 수에 도달하면 키값으로 그 층에서 내릴 사람을 찾고 자료구조에서 지움
Map<층 번호, List<승객 정보>> 형태로 설계하여, 키는 층 번호로, 값은 해당 층에서 내릴 승객 목록을 유지
map.get(층 번호)를 통해 해당 층에서 내릴 승객 리스트를 가져와 내리고 나면, 이 리스트를 지우거나 갱신

Map<Integer, List<Passenger>> floorPassengers = new HashMap<>();